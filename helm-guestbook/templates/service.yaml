apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-guestbook.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "helm-guestbook.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}

